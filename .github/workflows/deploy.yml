# Builds and publishes the game to GitHub Pages on every push to main.
# Nothing to run by hand: the implementing agent already pushes after each
# green group, so the live site follows the repo automatically.
#
# One-time repository setting required (Brian, in the browser):
#   Settings -> Pages -> "Build and deployment" -> Source: "GitHub Actions".
# Note: GitHub only serves Pages from a PRIVATE repository on paid plans;
# on a free plan the repository must be public for this to publish.

name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx tsc --noEmit
      - run: npm test
      - run: npm run build
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
